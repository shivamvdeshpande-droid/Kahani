name: Kahani Daily Stories

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'
    - cron: '0 11 * * *'
    - cron: '0 13 * * *'
    - cron: '0 15 * * *'

jobs:
  send-stories:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Kahani cron
        run: |
          curl -X POST "https://kahani-liard.vercel.app/api/cron-send" \
          -H "Authorization: Bearer kahani2024secret" \
          -H "Content-Type: application/json"
